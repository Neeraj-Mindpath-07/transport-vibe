import headerLogo from "@/assets/images/common/header-logo.svg";
import whisperMenu from "@/assets/images/common/whisper.svg";

/** Shared branding + chrome icons (replace remote Figma assets). */
export const layoutAssets = {
  headerLogo,
  whisperMenu,
} as const;
